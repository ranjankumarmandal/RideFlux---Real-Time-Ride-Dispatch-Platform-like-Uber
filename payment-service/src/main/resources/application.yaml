server:
  port: 8086

spring:
  application:
    name: payment-service

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/rideflux_payment
    username: postgres
    password: postgres

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/rideflux_payment
    user: postgres
    password: postgres

  redis:
    host: localhost
    port: 6379

  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    root: INFO