server:
  port: 8087

spring:
  application:
    name: notification-service

  kafka:
    bootstrap-servers: localhost:9092

  redis:
    host: localhost
    port: 6379

logging:
  level:
    root: INFO